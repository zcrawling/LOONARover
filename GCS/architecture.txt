   라즈베리파이                  Wi-Fi AP                  지상국 PC
┌─────────────────┐                                  ┌─────────────────┐
│ 카메라 → H.264   │ ───── 영상 / UDP 5600 ──────────→ │ 영상 플레이어    │
│ 1 / 3 / 5 Mbps  │                                  │ ffplay/GStreamer│
│                 │                                  │                 │
│ cFS GroundLink  │ ←──── 명령 / TCP 7443 ────────── │ GCS 백엔드       │
│ PAYLOAD(나중)   │ ───── 상태·명령 결과 / 동일 TCP ─→ │ ↕ HTTP/WebSocket│
│ MISSION (나중)   │                                  │ 조작·상태 화면(명령 확인 창)│
 Reaction Wheel(나중)

│                 │                                  │                 │
│ ROS 2  (나중)    │ ───── 관측 데이터 / DDS ─────────→ │ ROS 디버깅 도구  │
│ odom·경로·지도   │                                  │ RViz2 등        │
└─────────────────┘                                  └─────────────────┘
