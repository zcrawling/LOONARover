# Mesh provenance

`limo_base.dae` and `limo_wheel.dae` are unmodified assets from
`https://github.com/agilexrobotics/limo_ros2`, commit
`dcc5a8637f83236ed6778af86ad826e46a20cd92`, copied on 2026-09-03.

The upstream `limo_description/package.xml` declares its license as `TODO`.
These assets are retained only for LOONAR's local LIMO validation simulation;
do not redistribute them until AgileX supplies an explicit asset license.
